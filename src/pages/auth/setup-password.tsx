// src/pages/auth/setup-password.tsx
import React from 'react';
import SetupPassword from '@/components/Auth/SetupPassword';

const SetupPasswordPage: React.FC = () => {
  return (
    <main>
      <SetupPassword />
    </main>
  );
};

export default SetupPasswordPage;
